# Google Authentication — Smart Map Audit Report

**Project:** `vhgqzdxkjmsomclyrchv`  
**Date:** 2026-06-07  
**Scope:** Supabase Google OAuth + Next.js 15 App Router

---

## Executive summary

| Area | Status | Notes |
|------|--------|-------|
| **Authentication (email)** | ✅ Implemented | `signInWithPassword` on `/login` |
| **Google OAuth (frontend)** | ✅ **Fixed / implemented** | `GoogleSignInButton` + `signInWithOAuth` |
| **OAuth callback** | ✅ **Fixed / implemented** | `/auth/callback` with PKCE `exchangeCodeForSession` |
| **Session persistence** | ✅ Implemented | Supabase SSR cookies + `onAuthStateChange` |
| **Profile creation** | ⚠️ Partial | DB trigger + callback fallback; **migration required** |
| **Logout** | ✅ **Fixed / implemented** | Header `UserMenu` + `signOut()` |
| **Redirect URLs** | ⚠️ Dashboard config | Must be set in Supabase + Google Cloud Console |
| **Supabase Google provider** | ❓ Manual verify | Enable in Dashboard (see checklist below) |

---

## 1. Supabase Google OAuth configuration

### Required (Supabase Dashboard)

| Setting | Value |
|---------|--------|
| **Providers → Google** | Enabled |
| **Client ID** | From Google Cloud Console |
| **Client Secret** | From Google Cloud Console |

🔗 [Enable Google provider](https://supabase.com/dashboard/project/vhgqzdxkjmsomclyrchv/auth/providers?provider=Google)

### URL configuration

🔗 [URL Configuration](https://supabase.com/dashboard/project/vhgqzdxkjmsomclyrchv/auth/url-configuration)

| Field | Development | Production |
|-------|-------------|------------|
| **Site URL** | `http://localhost:3000` | `https://YOUR_DOMAIN` |
| **Redirect URLs** | `http://localhost:3000/auth/callback` | `https://YOUR_DOMAIN/auth/callback` |

Also add: `http://127.0.0.1:3000/auth/callback`

Set in app env:
```bash
NEXT_PUBLIC_APP_URL=https://YOUR_PRODUCTION_DOMAIN
```

### Google Cloud Console

Authorized redirect URI (Supabase handles OAuth):
```
https://vhgqzdxkjmsomclyrchv.supabase.co/auth/v1/callback
```

---

## 2. Frontend Google sign-in button

| Check | Status |
|-------|--------|
| Uses Supabase Auth | ✅ `supabase.auth.signInWithOAuth({ provider: 'google' })` |
| Component | ✅ `src/components/auth/google-sign-in-button.tsx` |
| Login page | ✅ `/login` |
| Register page | ✅ `/register` |
| Redirect target | ✅ `getAuthCallbackUrl(redirectPath)` |

---

## 3. OAuth callback handling

| Check | Status |
|-------|--------|
| Route | ✅ `src/app/auth/callback/route.ts` |
| PKCE code exchange | ✅ `exchangeCodeForSession(code)` |
| Error handling | ✅ Redirects to `/auth/auth-code-error` |
| Post-login redirect | ✅ `?redirect=/learn` (or custom path) |

**Flow:**
```
User → Google → Supabase → /auth/callback?code=... → session cookies → /learn
```

---

## 4. User session after login

| Check | Status |
|-------|--------|
| Cookie-based session (SSR) | ✅ `@supabase/ssr` in middleware |
| Client session hook | ✅ `useAuth()` → `getUser()` + `onAuthStateChange` |
| Middleware refresh | ✅ `updateSession()` on each request |

---

## 5. Profile creation after first login

| Mechanism | Status |
|-----------|--------|
| DB trigger `handle_new_user` | ✅ Migration `001` + improved `002` |
| Google metadata (`name`, `picture`) | ✅ Migration `002` |
| Callback fallback `ensureUserProfile` | ✅ `/auth/callback` |
| RLS insert policy | ✅ Migration `002` |

⚠️ **Run migrations** if `profiles` table missing:
```sql
-- Run 001_initial_schema.sql then 002_google_oauth_profiles.sql
```

---

## 6. Session persistence after refresh

| Check | Status |
|-------|--------|
| Supabase auth cookies | ✅ Set by SSR client |
| `useAuth` rehydrates on mount | ✅ |
| Middleware validates session | ✅ `getUser()` |

---

## 7. Logout

| Check | Status |
|-------|--------|
| `signOut()` | ✅ `src/lib/supabase/auth-actions.ts` |
| UI | ✅ Header `UserMenu` → Sign Out |
| Post-logout redirect | ✅ `/` + `router.refresh()` |

---

## 8. Redirect URLs (localhost + production)

Generated by `getRequiredRedirectUrls()`:

- `http://localhost:3000/auth/callback`
- `http://127.0.0.1:3000/auth/callback`
- `{NEXT_PUBLIC_APP_URL}/auth/callback` (when set)

---

## 9. Issues found & fixes applied

| Issue | Severity | Fix |
|-------|----------|-----|
| No Google sign-in button | 🔴 Critical | Added `GoogleSignInButton` |
| No OAuth callback route | 🔴 Critical | Added `/auth/callback` |
| No logout UI | 🟠 High | Added `UserMenu` |
| Google name/avatar not in profile trigger | 🟠 High | Migration `002` |
| No profile INSERT RLS for fallback | 🟡 Medium | Policy in `002` |
| No auth error page | 🟡 Medium | `/auth/auth-code-error` |

---

## 10. Remaining issues (manual steps)

| # | Issue | Owner | Action |
|---|-------|-------|--------|
| 1 | Google provider not verified in Dashboard | You | Enable Google + add OAuth credentials |
| 2 | Database schema may be missing | You | Run SQL migrations in Dashboard |
| 3 | Production domain not in Supabase redirects | You | Add production URL when deploying |
| 4 | `NEXT_PUBLIC_APP_URL` for production | DevOps | Set in Cloudflare/hosting env |
| 5 | End-to-end Google login test | QA | Test after steps 1–2 complete |

---

## Verification checklist

After Dashboard setup:

- [ ] Click **Continue with Google** on `/login`
- [ ] Complete Google consent screen
- [ ] Land on `/learn` (or redirect path)
- [ ] Header shows name + Sign Out
- [ ] Refresh page — still signed in
- [ ] Sign Out — returns to guest state
- [ ] Supabase Dashboard → Authentication → Users shows new user
- [ ] Table Editor → `profiles` row exists with Google name/avatar

---

## Code references

| File | Purpose |
|------|---------|
| `src/components/auth/google-sign-in-button.tsx` | Google OAuth button |
| `src/lib/supabase/auth-actions.ts` | `signInWithGoogle`, `signOut` |
| `src/app/auth/callback/route.ts` | OAuth callback |
| `src/lib/supabase/ensure-profile.ts` | Profile fallback |
| `src/hooks/use-auth.ts` | Session state |
| `src/components/auth/user-menu.tsx` | Login/logout UI |
| `supabase/migrations/002_google_oauth_profiles.sql` | Google profile support |

See also: `docs/GOOGLE_AUTH.md` for setup instructions.
